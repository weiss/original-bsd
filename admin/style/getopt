/*
 * Main/getopt(3) fragment.
 *
 *	@(#)getopt	5.1 (Berkeley) 06/29/91
 */

#include <sys/types.h>
#include <stdlib.h>

void usage __P((void));

main(argc, argv)
	int argc;
	char *argv[];
{
	int ch;

	while ((ch = getopt(argc, argv, "")) != EOF)
		switch(ch) {
		case '':
			break;
		case '?':
		default:
			usage();
		}
	argc -= optind;
	argv += optind;
}

void
usage()
{
	(void)fprintf(stderr, "usage: program:\n");
	exit(1);
}
